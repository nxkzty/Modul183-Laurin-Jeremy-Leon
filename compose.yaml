services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=railway'
      - 'POSTGRES_PASSWORD=${POSTGRES_PASSWORD}'
      - 'POSTGRES_USER=${POSTGRES_USERNAME}'
    ports:
      - '5432:5432'
